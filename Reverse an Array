class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        int a=0;
        int b=arr.size()-1;
        while (a<b){
            std::swap(arr[a],arr[b]);
            a++;
            b--;
        }
    }
};
